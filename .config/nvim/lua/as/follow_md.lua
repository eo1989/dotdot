--@EO borowed from jghauser/follow-md-links

local fn = vim.fn
local cmd = vim.cmd
local lopp = vim.loop
local ts_utils = require('nvim-treesitter.ts_utils')
local query = require('vim.treesitter.query')
local api = vim.api

local M = {}

local os_name = loop.os_name().sysname
local








return M

